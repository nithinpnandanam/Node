# Middlewares and Error Handlers

* ```Route Handlers```: A function that handles a specific route and sends the final response
* ```Middlewares``` : A function that runs before the route handler, and is used for things like logging, authentication, validation, or error handling.
* The ```401 status``` code in HTTP (Hypertext Transfer Protocol) means ```Unauthorized```