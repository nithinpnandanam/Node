# Mongoose

---
* There is a cluster that we created called ```dev-tinder-cluster```
* There can be many ```databases```in the ```cluster```
* we need to connect to a database in the cluster
* db will have a ```collection``` [user collection]
* collection will have ```documents```
---
* we should connect to a database and then start listening to the server
* if server starts listening and database is not connected then thats a problem
---
* instead of table we have collection
* we need to make a User collection
* ```at first schema``` of User collection needs to be made
* ```then a model``` needs to be made

---